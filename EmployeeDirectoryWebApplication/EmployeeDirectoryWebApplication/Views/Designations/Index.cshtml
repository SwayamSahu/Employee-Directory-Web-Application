@model IEnumerable<EmployeeDirectoryWebApplication.Models.Designation>

@{
    ViewData["Title"] = "Employee Designation";
}

<h1 class="my-4 text-center">Employee Designation</h1>

<div class="mb-3 text-right">
    <a class="btn btn-primary" asp-action="Create">Create New</a>
</div>

<div class="table-responsive">
    <table class="table table-striped table-bordered">
        <thead class="thead-dark">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.DesignationName)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.DesignationName)
                    </td>
                    <td class="text-center">
                    @if (User.IsInRole("Admin"))
                    {
                    
                        <a class="btn btn-info btn-sm" asp-action="Edit" asp-route-id="@item.DesignationId">
                            <span class="glyphicon glyphicon-pencil"></span> Edit
                        </a>
                    }
                        <a class="btn btn-primary btn-sm" asp-action="Details" asp-route-id="@item.DesignationId">
                            <span class="glyphicon glyphicon-info-sign"></span> Details
                        </a>
                            @if (User.IsInRole("Admin"))
                            {
                        <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@item.DesignationId">
                            <span class="glyphicon glyphicon-trash"></span> Delete
                        </a>
                            }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
